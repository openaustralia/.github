# Code owners for this repository.
#
# @openaustralia/staff has write access here, so GitHub can actually require
# its review. The previous entry named @openaustralia/code-reviewers, which
# holds access to no repository at all, so GitHub silently skipped it and the
# file enforced nothing.

* @openaustralia/staff
